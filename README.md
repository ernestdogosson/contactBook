# Terminal Contact Book

A terminal-based contact book built with TypeScript. It stores contacts with an id, name, email, and phone number. The app includes functions to add new contacts, list all contacts, search by name, and remove contacts by id.
